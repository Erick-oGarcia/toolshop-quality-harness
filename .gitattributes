# The repository stores and checks out LF on every platform. Without this,
# core.autocrlf=true rewrites the working tree to CRLF on Windows and
# `npm run format` fails locally while passing on the Linux CI runner.
* text=auto eol=lf
